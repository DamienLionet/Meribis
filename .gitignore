# Dépendances
node_modules/

# Sortie de build (régénérée par `npm run build:all`)
dist/

# Logs & divers
npm-debug.log*
*.log
.env
.env.*

# Polices source Aptos (livraison brute TTF + EULA) — seuls les woff2 réellement
# servis (src/assets/fonts/) sont versionnés. Aptos © Microsoft, police propriétaire.
/src/aptos/

# OS
.DS_Store
Thumbs.db
