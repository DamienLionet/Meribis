---
layout: layouts/base.njk
---
{% if heroImage or noHeroImage %}
{% set heroTitle = title %}
{% set heroLead = lead %}
{% include "partials/page-hero.njk" %}
<article class="mx-auto max-w-5xl px-6 py-12 md:py-16">
  <div class="prose-meritis">
    {{ content | safe }}
  </div>
</article>
{% else %}
<article class="mx-auto max-w-5xl px-6 py-12 md:py-16">
  {% include "partials/breadcrumbs.njk" %}

  <header class="mt-6">
    <h1 class="h1 text-brand-charcoal">{{ title }}</h1>
    {% if lead %}<p class="lead mt-4 text-brand-charcoal">{{ lead }}</p>{% endif %}
  </header>

  <div class="prose-meritis mt-8">
    {{ content | safe }}
  </div>
</article>
{% endif %}

{% if not noCta %}{% include "partials/cta-block.njk" %}{% endif %}
